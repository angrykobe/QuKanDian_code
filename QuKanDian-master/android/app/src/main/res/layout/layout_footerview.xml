<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout android:id="@+id/layout_footerview_content"
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:orientation="horizontal"
        android:gravity="center" >

	    <ProgressBar
	        android:id="@+id/foot_img"
	        style="?android:attr/progressBarStyleSmall"
	        android:layout_width="30dip"
	        android:layout_height="30dip"
	        android:layout_gravity="left|center_vertical"
	        android:indeterminate="true"
	        android:indeterminateDrawable="@drawable/list_foot_loading_progress"
	        android:indeterminateDuration="2000"
	        android:visibility="visible" />

	    <TextView
	        android:id="@+id/foot_text"
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:layout_gravity="center_vertical"
	        android:gravity="center"
	        android:paddingLeft="10dip"
	        android:text="@string/list_foot_loading_text"
	        android:textColor="@color/grey_dc"
	        android:textSize="15dp" />

    </LinearLayout>

	<LinearLayout android:id="@+id/layout_footerview_load_fail"
		android:layout_width="match_parent"
		android:layout_height="35dp"
		android:visibility="gone">

		<TextView
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:gravity="center"
			android:textSize="16dp"
			android:textColor="@color/grey_dc"
			android:text="加载失败,点击重试"/>

	</LinearLayout>

</RelativeLayout>
